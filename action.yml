name: 'markdownlint'
description: 'Run the ruby markdownlint'
inputs:
  parameters:  # id of input
    description: 'parameters for markdownlint'
    required: false
    default: '--help'
runs:
  using: 'docker'
  image: 'ruby.Dockerfile'
  args:
    - ${{ inputs.parameters }}
